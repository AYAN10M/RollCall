import 'package:flutter/material.dart';

class MyCheckInHistory extends StatelessWidget {
  const MyCheckInHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Check-In History'),
      ),
    );
  }
}